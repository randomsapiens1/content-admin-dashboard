/**
 * Single place to customize the admin dashboard's branding per project.
 */
export const branding = {
  siteName: process.env.NEXT_PUBLIC_SITE_NAME ?? "Content Admin Dashboard",
  tagline: "Content dashboard",
};
